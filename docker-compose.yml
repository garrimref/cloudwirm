services:
  mysql:
    image: mysql
    restart: always
    environment:
      MYSQL_DATABASE: 'cloudwirm_db'
      MYSQL_ROOT_PASSWORD: 'admin'
    ports:
      - '3306:3306'
    volumes:
      - mysql-data:/var/lib/mysql
volumes:
  mysql-data:
